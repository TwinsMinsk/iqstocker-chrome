{
  "name": "iqstocker-chrome-extension",
  "version": "1.0.0",
  "description": "Chrome extension for IQStocker Auto",
  "scripts": {
    "build": "node build/build.js",
    "watch": "node build/build.js --watch",
    "package": "npm run build && ./build/package-crx.sh",
    "package:win": "npm run build && .\\build\\package-crx.bat"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "fs-extra": "^11.1.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "canvas": "^3.2.0"
  }
}
