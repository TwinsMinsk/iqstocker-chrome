name: Deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Deploy to Railway
      run: |
        echo "Deployment will be handled by Railway auto-deploy"
        echo "Make sure Railway is connected to GitHub repository"

